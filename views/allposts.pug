extends _layout

block content
  .game
    each post in contentful.blogPosts
      .article
        h1=post.title
        span Publish Date: #{new Date(post.publishDate).toDateString()}
        !=markdown(post.trainingSummary)
        a(href=post._url) Read More